# v0.4.4 Release Notes

**Date:** 2026-04-06

## Fixes

Bootstrap CLAUDE.md now @-references the workspace-init skill directly from the payload (`@.workspace-update/.claude/skills/workspace-init/SKILL.md`). Previously, the CLAUDE.md only referenced two rules and told Claude to "follow hook instructions" — but Claude doesn't act on additionalContext from hooks reliably. The @-reference ensures Claude loads and follows the workspace-init skill on the first conversation turn.

## Contributors

- myron
